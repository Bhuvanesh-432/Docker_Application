apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: mysql-pvc
  namespace: greenleaf

spec:
  storageClassName: gp2

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 5Gi
